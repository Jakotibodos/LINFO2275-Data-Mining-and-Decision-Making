\documentclass[12pt, twocolumn]{article}

\usepackage{amsfonts,amsmath,amssymb}
\DeclareMathAlphabet{\mathpzc}{OT1}{pzc}{m}{it}
\usepackage[english]{babel}
\usepackage[style=alphabetic]{biblatex}\addbibresource{references.bib}
\usepackage{caption}
\usepackage{enumitem}
\usepackage[a4paper,left=1.5cm,right=1.5cm,top=2.5cm,bottom=2.5cm]{geometry}
\usepackage{float}
\pagestyle{plain}
\usepackage{geometry}
\usepackage{graphicx}
\usepackage{hyperref}
\hypersetup{
    colorlinks=true,
    citecolor=Bittersweet,
    filecolor=black,     
    linkcolor=Bittersweet,
    urlcolor= Bittersweet
}
\usepackage{mathtools}
\usepackage{nicematrix}
\usepackage{subcaption}
\usepackage{titling}
\usepackage[dvipsnames]{xcolor}

\renewcommand{\labelitemi}{$-$}
\renewcommand{\labelitemii}{$\circ$}

% \bibliographystyle{alpha}

\begin{document}
%%%%%%%%%%% TITLE %%%%%%%%%%%%%%%%%
\makeatletter
  \begin{titlepage}
  \parindent=0pt
Academic year 2025-2026 \hspace*{\stretch{0.75}} Ecole Polytechnique de Louvain\hspace*{\stretch{1}} LINFO2275

\hspace{5cm}

\vspace*{\stretch{1}}
\hrulefill
\begin{center}\bfseries\Huge
      
    LINFO2275 \\
    First project
\end{center}
\hrulefill
\vspace*{1cm}
\begin{center}\bfseries\Large
\underline{Group 2:}
\end{center}
    
\begin{center}\bfseries\Large
    Gavin Hope \texttt{} -- \\ 
    Jakob Thibodeau \texttt{12752500} -- DATI \\ 
    Lore Lernoux \texttt{77562000} -- MAP \bigskip \\

\end{center}
\begin{figure}[h!]
    \centering
    \includegraphics[scale=0.6]{img/image.png}
    \label{fig:logo2}
\end{figure}

    \vspace*{\stretch{2}}
    \vfill
        \includegraphics[scale=0.35]{img/logo_uclouvain.png}
        \hfill
        \centering Professor: Marco Saerens
        \hfill
        \includegraphics[height=0.045\textheight]{img/Logo_Ecole_Polytechnique_Louvain.jpg}
  \end{titlepage}
\makeatother

\newpage
%%%%%%%%%%% END TITLE %%%%%%%%%%%%%
\section*{Introduction}
In this project, we present the implementation of a Markov Decision Process to solve a Snakes and Ladders game. To do so, we implemented the value iteration algorithm to find the optimal policy for the game. This policy specifies, for each tile, the best dice to use in order to minimize the expected number of turns to reach the goal (tile 15). Afterwards, we compared this optimal policy to other suboptimal policies, such as fixed-dice policies, to see how they perform in terms of expected number of turns to win the game. Both theoretical results from value iteration and empirical results from Monte Carlo simulations are analyzed, presented and compared across different trap layouts, for both game configurations.

%\noindent To compare our theoretical results from value iteration with empirical results, we used Monte Carlo simulations. We simulated a large number of games using the optimal policy and computed the average number of turns to win the game, which should be close to the expected number of turns computed by value iteration. We also compared the optimal policy with sub-optimal policies by simulating games using those policies and computing their average number of turns to win the game.


\section*{Theoretical background}

The game is modeled as a Markov Decision Process. As seen during the lectures, it is defined by 
\begin{itemize}
    \item a set of states $S$, which in our case corresponds to the tiles of the board, i.e. $S = \{1, 2, \dots, 15\}$;
    \item a set of actions $A$, which corresponds to the choice of dice to roll, i.e. $A = \{1, 2, 3, 4\}$;
    \item a transition function $P(s' \mid s, a)$, which gives the probability of reaching state $s'$ from state $s$ after choosing action $a$. These probabilities are determined by the stochastic outcomes of the dice, the branching structure of the board, and the activation of traps;
    \item a cost function $c(s \mid a)$, which gives the cost when taking action $a$ in state $s$. In our case, we can define the cost as 1 for each turn taken, and 0 for reaching the goal (tile 15).
\end{itemize}

\noindent The goal is to find an optimal policy $\pi^*$ specifying, at each tile, which die minimizes the expected number of turns to reach tile 15 from any initial state. Let $V(s)$ be this total expected number of turns which respects Bellman's equation with as condition $V(15)=0$:
\begin{equation}
    V(s) = \min_{a \in A} \left[ c(s \mid a) + \sum_{s' \in S} P(s' \mid s, a) V(s') \right]
\end{equation}


\noindent This optimal expected number of turns can be computed using the value iteration algorithm, which iteratively updates $V(s)$ for each state until convergence. The optimal policy is then obtained by selecting, for each state, the action that minimizes the Bellman equation. \\

\noindent To validate the theoretical results, we performed Monte Carlo simulations. To do so, we simulated 1000 games from the initial state until the goal state following a policy, and we computed the empirical average number of turns. We also computed the standard deviation of these estimates. These are then compared to the theoretical values obtained through value iteration in order to validate the correctness of the optimal policy.

\section*{Implementation description}

\subsection*{Trap layouts}

We chose to use 6 different trap layouts to compare the optimal policy in different settings. These layouts are the following :
\begin{itemize}
    \item \textbf{No traps}: this layout has no traps, which means that the player can only move forward and never go back. This is the simplest layout and serves as a baseline for comparison.
    \item \textbf{Few traps}: this layout has a few traps scattered throughout the board.
    \item \textbf{Many traps}: this layout has many traps scattered throughout the board, which makes it more difficult to win the game.
    \item \textbf{Evil fast lane}: this layout has traps only on the fast lane.
    \item \textbf{Two-in-a-row traps}: this layout presents two segments with two consecutive trap tiles, making it more difficult to avoid them.
    \item \textbf{Back-to-three traps}: this layout has traps that send the player back to tile 3, which is the branching tile.

\end{itemize}

\subsection*{Sub-optimal strategies}
\noindent We chose to implement five sub-optimal strategies to compare with the optimal policy. These strategies are the following :
\begin{itemize}

    \item \textbf{One dice only}: for each of the 4 dice, this policy simply chooses one specific die. This gives us 4 sub-optimal policies to compare with the optimal one.
    \item \textbf{Uniform random strategy}: this policy chooses a die uniformly at random, independently of the current tile. This is a baseline strategy.

\end{itemize}


\section*{Results}
% We compared the optimal policy with the 5 sub-optimal strategies by simulating 1000 games for each policy and game structure (circle or not) and computing the average number of turns to win the game, i.e. the expected number of turns needed to reach tile 15 from tile 1. The results are summarized in the following tables, where ach table corresponds to a different trap layout. \\
% First, the baseline trap layout with no traps :

% \begin{table}[H]
% \centering
% \begin{tabular}{|c|c|c|}
%     \hline
%     Strategy & Circle & Linear \\
%     \hline
%     Optimal &  &  \\
%     Dice 1 Only &  &  \\
%     Dice 2 Only &  &  \\
%     Dice 3 Only &  &  \\
%     Dice 4 Only &  &  \\
%     Uniform Random &  &  \\
%     \hline
%     \end{tabular}
% \caption{Comparison of Strategies for the No Traps Layout}
% \end{table}
% We should then do one table per trap layout which seems huge, idk if you have another idea to present the results in a more concise way ?
\subsection*{Theoretical vs Empirical Results: Optimal Strategy}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.99\linewidth]{mdp_outputs/no_trap_layout.pdf}
    \caption{Expected number of turns to win the game for the no traps layout.}
    \label{fig:no_trap_layout}
\end{figure}

\noindent\autoref{fig:no_trap_layout} shows the optimal policy in the case where no traps are present, in both the circular and linear game structures. 

For tiles closer to the goal, the circle structure favors safer dice as we must stop exactly on the goal, while the linear structure favors faster dices as we can overshoot the goal. 
Since moving back to tile 1 is always a bad outcome, the optimal policy will always choose die 1 (the safe die) when the layout is circular, regardless of the trap layout.

For the rest of the tiles, the policy mostly favors die 3, as it has the highest expected value (1.5) of all dice, when traps are not used. 

Die 4 (the special die) is chosen on tile 1, since the player cannot move further back, bringing the expected value of the die to 2. Tiles 4 and 6 also favor the special die, since there is a 1/4 chance to move back directly to tile 3 and potentially move to the fast lane. 
We find it surprising that tile 3 itself does not favor the special die, as it could directly land on the goal tile.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.99\linewidth, trim={40pt 40pt 40pt 20pt}]{plots/no_trap_layout_circle.png}
    \includegraphics[width=0.99\linewidth, trim={40pt 40pt 20pt 30pt}]{plots/no_trap_layout_not_circle.png}
    \caption{Expected vs Average simulated number of turns to win by strategy for No Traps Layout}
    \label{fig:no_trap_layout_strategies}
\end{figure}

\noindent\autoref{fig:no_trap_layout_strategies} compares different strategies to the optimal policy. In the circle layout, the optimal policy far outperforms the other simpler strategies, since using riskier dice first and safer dice later brings a huge advantage to the player.
For the non-circle layout, the advantage is less significant, since repeatedly rolling die 3 is nearly the optimal policy anyway.
Note that for all layouts, looped or not, the "safe die only" strategy has an expected number of turns of 17. This value remains the same, as the safe die never triggers any traps and can move the player at most 1 tile, never overstepping the goal.


\textbf{MAY WANT TO CHANGE THIS PART}
\begin{figure}[H]
    \centering
    \includegraphics[width=0.99\linewidth]{mdp_outputs/few_traps_layout.pdf}
    \caption{Expected number of turns to win the game for the few traps layout.}
    \label{fig:few_traps_layout}
\end{figure}
\begin{figure}[H]
    \centering
    \includegraphics[width=0.99\linewidth, trim={40pt 40pt 40pt 20pt}]{plots/few_traps_layout_circle.png}
    \includegraphics[width=0.99\linewidth, trim={40pt 40pt 20pt 30pt}]{plots/few_traps_layout_not_circle.png}
    \caption{Expected vs Average simulated number of turns to win by strategy for Few Traps Layout}
    \label{fig:few_traps_layout_strategies}
\end{figure}
\noindent \autoref{fig:few_traps_layout} is more interesting as the game is more complex with the presence of traps. We can see that the optimal policy is again different in the two structures.
% See comment in TODO



\begin{figure}[H]
    \centering
    \includegraphics[width=0.99\linewidth]{mdp_outputs/many_traps_layout.pdf}
    \caption{Expected number of turns to win the game for the many traps layout.}
    \label{fig:many_traps_layout}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.99\linewidth, trim={40pt 40pt 40pt 20pt}]{plots/many_traps_layout_circle.png}
    \includegraphics[width=0.99\linewidth, trim={40pt 40pt 20pt 30pt}]{plots/many_traps_layout_not_circle.png}
    \caption{Expected vs Average simulated number of turns to win by strategy for Many Traps Layout}
    \label{fig:many_traps_layout_strategies}
\end{figure}
As see in \autoref{fig:many_traps_layout}, having many traps unsurprisingly leads to the near exclusive use of the safe die.
As the safe die moves the player by at most 1 tile, having the layout loop or not doesn't affect the optimal policy choice or its effectiveness. 
This is further shown by comparing to the only die 1 strategy, which takes almost the same number of turns as the optimal strategy.


\textbf{MIGHT WANT TO REDO THE EVIL FAST LANE WITH ONLY RESTART TRAPS}
\begin{figure}[H]
    \centering
    \includegraphics[width=0.99\linewidth]{mdp_outputs/evil_fast_lane_layout.pdf}
    \caption{Expected number of turns to win the game for the evil fast lane layout.}
    \label{fig:evil_fast_lane_layout}
\end{figure}
\begin{figure}
    \centering
    \includegraphics[width=0.99\linewidth, trim={40pt 40pt 40pt 20pt}]{plots/evil_fast_lane_layout_circle.png}
    \includegraphics[width=0.99\linewidth, trim={40pt 40pt 20pt 30pt}]{plots/evil_fast_lane_layout_not_circle.png}
    \caption{Expected vs Average simulated number of turns to win by strategy for evil fast lane Layout}
    \label{fig:evil_fast_lane_layout_strategies}
\end{figure}

\noindent \autoref{fig:evil_fast_lane_layout} 

\begin{figure}[H]
    \centering
    \includegraphics[width=0.99\linewidth]{mdp_outputs/two_in_a_row_layout.pdf}
    \caption{Expected number of turns to win the game for the two-in-a-row traps layout.}
    \label{fig:two_in_a_row_traps_layout}
\end{figure}



\begin{figure}[H]
    \centering
    \includegraphics[width=0.99\linewidth, trim={40pt 40pt 40pt 20pt}]{plots/two_in_a_row_layout_circle.png}
    \includegraphics[width=0.99\linewidth, trim={40pt 40pt 20pt 30pt}]{plots/two_in_a_row_layout_not_circle.png}
    \caption{Expected vs Average simulated number of turns to win by strategy for two-in-a-row Layout}
    \label{fig:two_in_a_row_traps_layout_strategies}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.99\linewidth]{mdp_outputs/back_to_3_layout.pdf}
    \caption{Expected number of turns to win the game for the back-to-three traps layout.}
    \label{fig:back_to_three_layout}
\end{figure}
In the back-to-3 layout shown in \autoref{fig:back_to_three_layout}, the optimal strategy becomes much more sophisticated. In the looped layout, the player would much rather take the fast lane (as evidenced by the choice of the safe die on tile 2) since it avoids having to tiptoe past the restart trap on tile 10.
Surprisingly, the optimal strategy chooses the risky die at tile 8, risking triggering the restart trap. It appears that the risk of restarting is worth taking over moving slowly (with the safe die) to the goal.
The player in the non-looped layout doesn't benefit as much from the fast lane, as it can simply leap over tile 10 to reach the goal with the special die.
\begin{figure}[H]
    \centering
    \includegraphics[width=0.99\linewidth, trim={40pt 40pt 40pt 20pt}]{plots/back_to_3_layout_circle.png}
    \includegraphics[width=0.99\linewidth, trim={40pt 40pt 20pt 30pt}]{plots/back_to_3_layout_not_circle.png}
    \caption{Expected vs Average simulated number of turns to win by strategy for back-to-three Layout}
    \label{fig:back_to_three_layout_strategies}
\end{figure}
Again, the optimal policy outperforms all other strategies. Having the layout loop or not clearly affects how risk-averse the player can be, as the risky die and the special die policies are nearly twice and thrice as fast, respectively.

\subsection*{Theoretical vs Empirical Results: Sub-optimal Strategies}
We compared the optimal policy with the 5 sub-optimal strategies by simulating 1000 games for each policy and game structure (circle or not) and computing the average number of turns to win the game, i.e. the expected number of turns needed to reach tile 15 from tile 1. For each trap layout, we represented the game and the optimal policy by coloring the tiles according to the dice that should be used on that tile. 


\section*{Conclusion}

\end{document}
